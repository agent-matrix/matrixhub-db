# Format: "username" "md5<md5(password+username)>"
# Example (do not commit real hashes):
# "matrix" "md5d8578edf8458ce06fbc5bb76a58c5ca4"
#
# Generate userlist.txt if you prefer static auth:
# printf '"matrix" "md5%s"\n' "$(printf '%s' "${POSTGRES_PASSWORD}${POSTGRES_USER}" | md5sum | awk '{print $1}')" > pgbouncer/userlist.txt
